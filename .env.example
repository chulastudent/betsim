# Set this in Vercel's Project Settings → Environment Variables.
# Use Neon’s pooled connection string in production.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require

# Server-side football-data.org token. Never expose this in browser code.
FOOTBALL_DATA_TOKEN=your_football_data_token
